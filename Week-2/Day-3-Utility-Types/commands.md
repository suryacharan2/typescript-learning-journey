# Commands Used - Day 3 (Week 2)

| Command | Explanation |
|---------|-------------|
| `npx ts-node examples.ts` | Runs the examples file directly |
| `tsc --lib es2020` | Ensures the compiler includes the standard library definitions where utility types like `Partial` and `Record` are defined |
