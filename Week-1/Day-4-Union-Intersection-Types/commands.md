# Commands Used - Day 4

| Command | Explanation |
|---------|-------------|
| `npx ts-node assignment.ts` | Runs the assignment file directly |
| `tsc --strictNullChecks` | Ensures `null` and `undefined` are only allowed where explicitly typed, which helps with safer narrowing |
