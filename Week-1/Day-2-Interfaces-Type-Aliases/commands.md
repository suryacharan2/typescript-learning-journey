# Commands Used - Day 2

| Command | Explanation |
|---------|-------------|
| `npx ts-node examples.ts` | Runs the examples file directly |
| `tsc --noEmit` | Type-checks the project without producing compiled JS files, useful for quickly checking for errors |
| `tsc examples.ts --strict` | Compiles a file with strict type checking enabled |
